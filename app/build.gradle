apply plugin: 'com.android.application'
apply plugin: 'kotlin-parcelize'

def keystorePropertiesFile = rootProject.file("keystore.properties")
def keystoreProperties = new Properties()
def hasSigningConfig = keystorePropertiesFile.exists()
if (hasSigningConfig) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}

// Google's official sample/test AdMob IDs (https://developers.google.com/admob/android/test-ads).
// Safe to ship: they never serve real ads or earn revenue. Debug builds always use these.
// Release builds use these same IDs as a fallback UNLESS admob.properties (gitignored, never
// committed) supplies real production IDs - see admob.properties.example.
def testAdmobAppId = "ca-app-pub-3940256099942544~3347511713"
def testBannerAdUnitId = "ca-app-pub-3940256099942544/6300978111"

def admobPropertiesFile = rootProject.file("admob.properties")
def admobProperties = new Properties()
def hasProductionAdmobConfig = admobPropertiesFile.exists()
if (hasProductionAdmobConfig) {
    admobPropertiesFile.withInputStream { admobProperties.load(it) }
}
def releaseAdmobAppId = admobProperties.getProperty("admobAppId", testAdmobAppId)
def releaseBannerAdUnitId = admobProperties.getProperty("bannerAdUnitId", testBannerAdUnitId)

android {
    signingConfigs {
        if (hasSigningConfig) {
            release {
                keyPassword keystoreProperties['keyPassword']
                keyAlias keystoreProperties['keyAlias']
                storeFile file(keystoreProperties['storeFile'])
                storePassword keystoreProperties['storePassword']
            }
        }
    }
    compileSdk 37
    defaultConfig {
        applicationId "com.tb.tetrisbrick.game"
        minSdkVersion 24
        targetSdkVersion 36
        versionCode 2
        versionName "2.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        debug {
            resValue "string", "admob_app_id", testAdmobAppId
            resValue "string", "banner_ad_unit_id", testBannerAdUnitId
            buildConfigField "boolean", "ADS_CONFIGURED_FOR_RELEASE", "false"
        }
        release {
            minifyEnabled true
            shrinkResources true
            if (hasSigningConfig) signingConfig signingConfigs.release
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            resValue "string", "admob_app_id", releaseAdmobAppId
            resValue "string", "banner_ad_unit_id", releaseBannerAdUnitId
            buildConfigField "boolean", "ADS_CONFIGURED_FOR_RELEASE", hasProductionAdmobConfig.toString()
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    buildFeatures {
        viewBinding true
        buildConfig true
        resValues true
    }
    testOptions {
        unitTests {
            includeAndroidResources = true
        }
    }
    namespace 'com.tb.tetrisbrick.game'
}

dependencies {
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.7.0'
    androidTestImplementation 'androidx.test.ext:junit:1.3.0'
    implementation 'androidx.appcompat:appcompat:1.8.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.2.2'
    testImplementation 'junit:junit:4.13.2'
    implementation 'io.github.ShawnLin013:number-picker:2.4.13'
    implementation 'androidx.work:work-runtime:2.11.2'
    implementation 'androidx.work:work-runtime-ktx:2.11.2'
    implementation 'androidx.lifecycle:lifecycle-process:2.11.0'

    implementation "androidx.core:core-ktx:1.19.0"
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"

    testImplementation 'org.mockito:mockito-core:5.23.0'
    testImplementation 'org.robolectric:robolectric:4.16'
    androidTestImplementation "org.jetbrains.kotlin:kotlin-test-junit:$kotlin_version"
    implementation 'com.google.android.gms:play-services-ads:25.5.0'
    implementation 'com.google.android.ump:user-messaging-platform:4.0.0'
}
repositories {
    google()
    mavenCentral()
}

